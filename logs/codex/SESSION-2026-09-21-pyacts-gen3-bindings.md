# SESSION-2026-09-21-pyacts-gen3-bindings — SoftEng capability assessment

## Scope and evidence

Contemporaneous bounded assessment, starting at
`8472892bee490a7e04e58bab7d1332493cb615b9` on
`study/pyacts-gen3-bindings`. The pre-existing untracked
`tools/tracker_layout/__pycache__/covariance_control.cpython-314.pyc` is unrelated
and preserved. Exact tokens, client version, thread ID and actual conversation
start time are unavailable. The paired JSON inventories changes and checks.

## Selected conversation

- User request (paraphrase): put tracker-placement work on hold; ask SoftEng to
  determine whether the latest pip pyacts release can place module surfaces in
  layers and build a Gen-3 tracking geometry.
- User authorization (exact quote): “all `gh api commands that read are allowed` - go on!”
- Assistant outcome: installed a separate wheel environment, delegated primary
  source inspection to the software role, and tested finite sensitive module
  construction plus independent straight-ray navigation checks.

## Decisions and outcomes

The live package index and PyPI JSON report 47.7.0. The wheel supports a tested
JSON-sensitive-surface → LayerBlueprintNode → Blueprint.construct route.
Two barrel and two disk layers contain 32 synthetic modules. TryAll and
SurfaceArray each matched independent planar intersections for the same 200
seeded rays, with 45 crossings each. This completes only the binding assessment;
DES-005 TRK-SE03 and physical tracker studies remain incomplete.

Direct Transform3(translation, rotation) produced nonfinite values; pinned source
shows uninitialized storage. Angle-axis composition and JSON transforms work.
Direct geometry IDs do not make surfaces sensitive, but JSON sensitivity does.
The blueprint children getter also fails. Limitations and reproduction commands
are retained in the assessment, with public source ID SRC-PYACTS-4770-BINDINGS.
No installed wheel/upstream source was patched. No upstream message was sent.

## Commands and validation

The paired JSON records actual commands and results. Relevant investigation used
read-only gh api calls for pinned ACTS binding sources, live PyPI metadata,
pip index/install and runtime API probes. A sandbox DNS failure was corrected by
an authorized network retry. An early diagnostic without an origin-containing
volume aborted; an initial hollow-disk/solid-barrel stack failed portal gluing.
Explicit inner empty fixture volumes resolved both topology requirements.
These failed exploratory attempts are not detector failures or accepted evidence.

An independent SoftEng review found no blocking correctness concerns; the
suggested primary JSON converter sources were added to the catalogue. Logging
tests (15), dashboard tests (25), dashboard validation/build, staged whitespace
checks and the retained-hash/link audit passed.

Retained results include wheel, executable and fixture hashes, seed, tolerances,
counts and platform. Only macOS arm64 / CPython 3.14.6 was exercised. No field
propagation, reconstruction, material physics or detector acceptance is claimed.

## Changes and revision links

Added an isolated prototype tool, pinned requirement, reproduction guide and
Markdown/JSON assessment; updated documentation index, source catalogue, tracking
and this session pair. Tracker proposals in PR #13 remain on hold. No production
geometry or design approval state changed. No PR is published by this assessment.

## Follow-up

When the user resumes tracker work, test one actual module family, inactive masks,
stereo semantics and field propagation. Physical module/material/field inputs
and full simulation remain separate dependencies. Human design review and sign-off
remain required before production integration; no approval is inferred here.
